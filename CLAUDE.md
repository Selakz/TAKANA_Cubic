# TAKANA_Cubic代码规范

本项目为一个音乐游戏及其关卡编辑器，游戏特点是音符只出现在轨道上。

请使用中文回答问题，但在代码中禁止使用中文字符，包括注释内。

涉及到事件或响应式数据的定义时，优先使用[](Assets/Scripts/T3Framework/Static/Event/NotifiableProperty.cs)；可以将其视为一个简化版的reactive实现。

无需进行任何编译操作，用户会自行审查所有代码并修复简单的语法错误。无需关心具体场景测试与验证。

## 全局代码规范

- 每个文件的第一行必须是`nullable enable`。
- 遵循C#标准命名规范，但任何字段都不要以下划线开头。
- 能简化的new操作优先简化，如使用`List<T> list = new()`而不是`List<T> list = new List<T>()`。
- 对在定义处不能给定初始值，但能保证通过注入或`[SerializeField]`或`Awake()`方法初始化的字段，使用`default!`在定义处初始化。如果遇到使用`default!`进行初始化的字段，则认为它一定不为空，无需进行任何空检查和保底措施。
- 禁止任何通过硬编码路径加载资源的做法
- 如果写出来的某个私有工具方法目前只在另一个方法内被调用，将其作为该方法的局部函数写在该方法末尾。

## 代码分类

项目遵循数据驱动架构，代码主要分为以下几类，请根据任务需求选择代码类，然后再进一步遵循其指向的子规范文件里的规范。

- 配置类：存储可配置数值，见[](Docs/Config.md)
- 数据模型类：游戏物体中可持久化的、通常不在运行时改变的数据。见[](Docs/Model.md)
- 游戏组件类：存储数据模型以及游戏物体中常随着游戏进度动态改变的数据。见[](Docs/Component.md)
- 视图类：游戏组件对应的在世界里的具体实体。UI元素见[](Docs/UI.md)，游戏物体见[](Docs/View.md)
- 系统类：通过处理游戏组件数据和视图，实现具体的业务逻辑。见[](Docs/System.md)
- 工具类：提供通用的功能，如通用算法、拓展方法等。见[](Docs/Tool.md)

## 任务上下文

根据具体任务，进一步选择性阅读以下文档了解与任务相关的上下文。

- 编辑器热重载插件：见[](Docs/EditorPluginBridge.md)